## Setup.py : "this is an essential part of packageing and distributing python project . it is used by setuptools (or distutils in older python         versions) to define the configuration of the project,such as its metadata,dependencies,and more"

code for setup
## Activate the .venv 
## pip install -e .
## python setup.py build
## pip install -r requirements.txt